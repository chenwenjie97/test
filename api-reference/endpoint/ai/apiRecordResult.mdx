---
title: '获取任务结果'
openapi-ai: 'GET /apiRecord/result'
description: '通过收集任务ID来获取任务执行结果'
---

## 描述

此端点用于获取先前提交的任务的执行结果。客户端可以通过轮询此端点来检查任务状态。

## 参数

- `limit` (可选, integer): 指定返回结果的最大数量。

## 响应

- `200`: 成功返回植物列表
- `400`: 请求参数错误